# React-Experiment-7-Rendering-a-List-using-map-with-Unique-Keys
To render a list of students dynamically using the **map()** function and assign a **unique key prop** to each item for efficient rendering and reconciliation in React.
